# PulseBrief 下一阶段任务清单

## 当前阶段目标

先把 Flutter 高保真 UI 精修成果稳定下来，再进入 8 页面截图验收与后端 V1 接口骨架。每一步都保留可验证结果，避免 UI、资产、后端联调混在一个不可回退的大改动里。

## 阶段 1：当前 UI 精修成果归档

- [x] 复核当前工作区变更范围，只包含 UI 精修、透明插图资产、文档和生成脚本。
- [x] 运行 Flutter 质量检查：`flutter analyze`。
- [x] 运行 Flutter 测试：`flutter test`。
- [x] 构建 Android debug APK：`flutter build apk --debug --target-platform android-x64`。
- [x] 将当前 UI 精修成果提交为一个 Git 里程碑。

验收标准：

1. `flutter analyze` 无问题。
2. `flutter test` 全部通过。
3. Android debug APK 构建成功。
4. Git commit 内容边界清晰。

## 阶段 2：8 页面模拟器截图验收

- [x] 登录页截图。
- [x] 首页截图。
- [x] 分类页截图。
- [x] 订阅页截图。
- [x] 资讯详情页截图。
- [x] 每日简报页截图。
- [x] 语音播放器页截图。
- [x] 我的页面截图。
- [x] 输出截图验收记录，标记每页 P0/P1/P2/P3 问题。

验收标准：

1. 8 个页面都能在 Android 模拟器正常打开。
2. 无明显溢出、遮挡、错位。
3. 底部导航、底部操作栏和安全区正常。
4. 截图文件保存到 `.codex/screenshots/` 或同等验收目录。

## 阶段 3：截图问题逐页修正

- [x] 修正 P0：页面无法打开、严重遮挡、严重溢出。
- [x] 修正 P1：首屏布局明显不符合原型、主按钮或导航错位。
- [x] 修正 P2：卡片密度、图片比例、文字层级、间距问题。
- [x] 记录暂不处理的 P3：细微阴影、个别插图风格、状态栏差异。
- [x] 再次运行 `flutter analyze`、`flutter test`、Android debug APK 构建。

验收标准：

1. P0/P1/P2 问题清零或有明确说明。
2. 关键页面截图效果接近原型。
3. 自动化验证通过。

阶段结果：已完成第一轮模拟器截图验收和 P2 排版修正，详见 `docs/Flutter高保真UI截图验收记录.md`。

## 阶段 4：文档与本机预览说明

- [x] 更新根目录 README，说明当前 Flutter UI 状态。
- [x] 更新 mobile README，补充 Android Studio、SDK、模拟器和 Flutter 运行命令。
- [x] 补充透明插图资产生成说明。
- [x] 记录当前 APK 构建路径和本机预览方式。

验收标准：

1. 新成员可以按 README 在本机启动模拟器预览 APP。
2. GitHub 首页能看出项目已切换到 Flutter 主线。
3. 不再突出已经移除的 React/Vite PWA。

阶段结果：已更新根目录 README 和 `mobile/README.md`，本机预览以 Android 模拟器为主。

## 阶段 5：Spring Boot + MySQL V1 接口骨架

- [x] 设计 API 契约：分类、文章、详情、简报、订阅、收藏、播放历史。
- [x] 补充后端实体和 DTO。
- [x] 补充 Flyway migration。
- [x] 补充 Controller / Service / Repository。
- [x] 补充后端测试。
- [x] Flutter 新增 Repository 层，先保持 mock 与 API 可切换。

验收标准：

1. 后端测试通过。
2. Flutter UI 不直接散落真实接口调用。
3. mock 数据可以平滑替换为 Spring Boot API。

阶段设计：已新增 `docs/api/V1核心接口设计.md`，并按确认后的 V1 核心接口完成后端骨架与 Flutter 数据层衔接。

阶段后端结果：已完成 V1 用户端后端 API 骨架，包括登录、游客模式、分类、首页资讯、资讯列表、资讯详情、今日简报、简报详情、订阅保存、收藏和播放历史。后端测试 16 个通过，并已用本地 MySQL 完成读写接口冒烟。

阶段 Flutter 数据层结果：已新增 `PulseRepository`、mock/API 双实现、Repository 注入、登录/首页/分类/简报/订阅页初始接入，并通过 `flutter analyze`、`flutter test` 和 Android debug APK 构建。

## 阶段 6：Flutter API 模式联调

- [x] 启动 MySQL 与 Spring Boot 本地 API。
- [x] 用真实 HTTP 冒烟验证登录、分类、首页、列表、详情、简报、订阅、收藏、播放历史。
- [x] 修复订阅保存重复唯一键冲突。
- [x] Flutter 新增 live API Repository 测试，默认跳过，联调时显式开启。
- [x] Flutter transport 显式使用 UTF-8 写入 JSON 请求体。
- [x] Android 模拟器使用 `10.0.2.2:8080` 运行 API 模式 APP。
- [x] 截图/记录 API 模式下 8 页面页面级验收结果。

阶段记录：详见 `docs/Flutter API模式联调记录.md`。

阶段结果：已完成 API 模式 8 页面截图验收，P0/P1 未发现问题；已修复 API 时间展示、分类首屏空内容和简报图标识别问题。剩余 P3 为“我的”页用户统计仍使用本地 mock，后续接用户中心 API 时处理。

## 阶段 7：用户中心 API 与我的页真实数据

- [x] 补充用户中心资料接口设计：`docs/用户中心资料接口设计.md`。
- [x] 新增 `GET /api/user/profile`，返回用户资料、订阅数、收藏数、阅读数和播放数。
- [x] 后端统计接入 `user_subscription`、`user_favorite`、`user_play_history`。
- [x] Flutter Repository 新增 `getUserProfile()`，支持 mock/API 双实现。
- [x] MinePage 改为通过 Repository 加载资料，并补充加载态与错误态。
- [x] 补充后端 Controller/Service 测试、Flutter mock/API/live 测试。
- [x] 运行后端测试、Flutter analyze、Flutter test、live API 测试和 API 模式 APK 构建。

验收标准：

1. `GET /api/user/profile` 未登录返回 401，登录后返回资料与统计字段。
2. API 模式“我的”页不再直接读取 `mockUser`。
3. mock 模式仍可独立运行。
4. `flutter analyze`、`flutter test`、`mvnw test` 全部通过。

阶段结果：已完成用户中心资料接口和 Flutter 我的页真实数据接入，阶段 6 遗留的 P3 已关闭；阅读历史统计本轮按设计暂返回 `0`，后续阅读历史模块补齐。

## 阶段 8：收藏列表、阅读历史与播放历史

- [x] 补充用户历史列表接口设计：`docs/用户历史列表接口设计.md`。
- [x] 新增 `GET /api/user/favorites` 收藏列表接口。
- [x] 新增 `POST /api/user/read-history` 和 `GET /api/user/read-history`。
- [x] 新增 `GET /api/playback/history` 播放历史列表接口。
- [x] 文章详情登录态下记录阅读历史。
- [x] 用户资料 `readCount` 改为读取 `user_read_history`。
- [x] Flutter Repository 新增收藏列表、阅读历史、播放历史和阅读记录方法。
- [x] “我的收藏”“阅读历史”“播放历史”入口可打开独立 Flutter 页面。
- [x] 补充后端、Flutter、live API 测试。

验收标准：

1. 三个用户列表接口登录后可读取。
2. 文章详情打开后可产生阅读历史。
3. “我的”页三个入口可跳转真实页面。
4. mock 模式和 API 模式均可运行。
5. 后端测试、Flutter analyze、Flutter test、live API 测试和 API 模式 APK 构建通过。

阶段结果：已完成 V1 用户中心列表闭环，用户可以从“我的”页进入收藏、阅读和播放历史。V1 暂不做批量删除、清空历史和统一分页响应。

## 阶段 9：分页规范与列表管理

- [x] 补充分页规范与列表管理设计：`docs/分页规范与列表管理设计.md`。
- [x] 新增后端统一 `PageResponse<T>`。
- [x] 收藏、阅读历史、播放历史列表改为分页响应。
- [x] 新增 `DELETE /api/user/read-history` 清空阅读历史。
- [x] 新增 `DELETE /api/playback/history` 清空播放历史。
- [x] Flutter API Repository 兼容分页响应 `items`。
- [x] 收藏列表支持取消收藏并从列表移除。
- [x] 阅读历史和播放历史页面支持清空。
- [x] 列表页面支持下拉刷新。

验收标准：

1. 三个用户列表接口返回 `items/page/pageSize/total/hasMore`。
2. 阅读历史和播放历史清空接口可用。
3. Flutter mock/API 模式均能打开列表并展示管理入口。
4. 后端测试、Flutter analyze、Flutter test、live API 测试和 API 模式 APK 构建通过。

阶段结果：已完成 V1.1 列表分页结构和最小管理能力；本轮暂不做无限滚动、批量多选和单条历史删除。

## 阶段 10：OpenAPI 与 Swagger 在线接口文档

- [x] 补充 OpenAPI 与 Swagger 接口文档设计：`docs/OpenAPI与Swagger接口文档设计.md`。
- [x] 新增 springdoc-openapi 依赖。
- [x] 新增 OpenAPI 基础信息、服务地址、标签和开发态 Bearer Token 安全方案。
- [x] 配置 `/swagger-ui.html` 和 `/v3/api-docs`。
- [x] 限定 OpenAPI 扫描 `/api/**`。
- [x] 新增 `OpenApiDocumentationTest`，验证文档 JSON 和关键 V1 路径。
- [x] 更新 `docs/api/README.md` 和 `backend/README.md`。

验收标准：

1. 本地后端启动后可访问 `http://localhost:8080/swagger-ui.html`。
2. 本地后端启动后可访问 `http://localhost:8080/v3/api-docs`。
3. OpenAPI JSON 包含 `/api/health`、`/api/auth/login`、`/api/articles/home` 等关键路径。
4. OpenAPI JSON 包含 `bearerAuth` 安全方案。
5. 后端测试、Flutter analyze、Flutter test 通过。

阶段结果：已完成 V1 在线接口文档入口，后续可按模块继续补充 `@Operation`、`@Parameter` 和 `@Schema` 示例，让 Swagger UI 更适合业务联调。

## 阶段 11：真实资讯采集 V1 设计

- [x] 补充整体任务清单：`docs/真实资讯采集与Admin审核发布整体任务清单.md`。
- [x] 补充真实资讯采集 V1 设计：`docs/真实资讯数据采集V1设计.md`。
- [x] 明确 V1 采集合规边界：不抓取付费研报、登录后 PDF、未授权全文转载和需要绕过访问控制的内容。
- [x] 明确 V1 只采集最新内容，不做历史批量回溯。
- [x] 明确 V1 发布边界：采集内容必须进入候选池，经过 Admin 审核后才发布到用户端。
- [x] 设计原始资讯池、候选资讯池、采集任务日志和采集源配置。
- [x] 设计 Provider 抽象、去重策略、状态流转和测试方案。

验收标准：

1. 文档覆盖背景、目标、非目标、影响范围、数据模型、权限模型、后端实现方案、前端影响、测试方案和风险分阶段建议。
2. 第一轮只选择一个低风险数据源；默认不直接抓取网页全文，只有授权明确且属于最新窗口的来源才允许采集正文片段、授权全文或授权 PDF。
3. 用户端仍只读取已发布内容，候选内容不进入用户端。

阶段结果：已完成真实资讯采集 V1 的整体设计边界。下一步进入采集表结构、Provider 抽象与第一个低风险采集器的后端实现设计。

## 阶段 12：采集适配层与首个 Provider

- [x] 新增采集源抽象 `NewsIngestionProvider`。
- [x] 新增统一采集返回模型 `RawNewsPayload`。
- [x] 新增采集请求模型 `IngestionRequest`。
- [x] 新增采集源配置模型 `IngestionProperties`。
- [x] 第一批接入低风险 `FIXTURE` Provider，不请求真实外网。
- [x] Provider 测试使用固定内置 fixture，不依赖真实外网。
- [x] 采集配置默认关闭：`PULSEBRIEF_INGESTION_ENABLED=false`。
- [x] Provider 和配置均已纳入 Spring 应用上下文测试。

验收标准：

1. 无真实 API Key 时测试仍可通过。
2. Provider 失败不会影响现有用户端 API。
3. 新增配置项不提交密钥。

阶段结果：已完成采集适配层、统一 payload、请求模型、配置模型和 fixture Provider。下一步进入阶段 13：采集入库、任务日志与去重。

## 阶段 13：采集入库、任务日志与去重

- [x] 新增 Flyway migration：采集源、采集任务日志、原始资讯池。
- [x] 新增采集领域 Entity：`NewsIngestionSource`、`NewsIngestionJob`、`RawNewsItem`。
- [x] 新增 Repository：采集源、采集任务、原始资讯。
- [x] 新增 `IngestionDeduplicationService`，支持 URL 哈希与内容指纹。
- [x] 新增 `RawNewsIngestionService`，支持原始资讯写入。
- [x] 实现 URL 去重。
- [x] 实现标题指纹去重。
- [x] 实现采集任务统计：拉取、新增、重复、候选数量。
- [x] 补充后端测试。

验收标准：

1. 相同原文链接不会重复入库。
2. 相同来源、标题和日期不会重复入库。
3. 每次采集都有任务日志。

阶段结果：已完成采集入库、任务日志和去重基础能力。V3 migration 已应用到本地 MySQL。

## 阶段 14：候选资讯生成

- [x] 新增候选资讯表。
- [x] 从原始资讯生成候选资讯。
- [x] 自动填充标题、摘要、来源、原文链接、发布时间和初始分类。
- [x] 未审核候选不进入用户端。
- [x] 补充候选生成测试。

验收标准：

1. 原始资讯可生成候选。
2. 候选状态为待审核。
3. 用户端文章 API 不返回候选内容。

阶段结果：已完成 `candidate_article` V4 migration、候选生成服务、初始分类规则和发布隔离测试。下一步进入阶段 15：Admin API 审核发布设计。

## 阶段 15：Admin API 审核发布设计

- [x] 补充 Admin 审核发布 V1 设计文档。
- [x] 设计 `/api/admin/ingestion/**`。
- [x] 设计 `/api/admin/candidates/**`。
- [x] 设计 `/api/admin/articles/**`。
- [x] 设计开发态 Admin 鉴权。
- [x] OpenAPI 增加 Admin API 分组。

验收标准：

1. Admin API 与用户端 API 边界清晰。
2. 写操作需要 Admin 鉴权。
3. Swagger 可以查看 Admin API。

阶段结果：已完成 `docs/Admin审核发布V1设计.md`，明确 Admin Token、候选列表/详情/编辑/拒绝/发布、文章下线、简报管理边界、OpenAPI 分组和测试策略。下一步进入 Admin API 实现或 React Admin 工程骨架前，需要先确认该设计。

## 阶段 16：React Admin 工程骨架

- [ ] 新建 `admin/` React + Vite + TypeScript 工程。
- [ ] 配置 lint / test / build。
- [ ] 建立专业后台布局。
- [ ] 建立 Admin API client。
- [ ] 建立路由：Dashboard、采集任务、候选资讯、文章管理、分类管理、简报管理。
- [ ] 更新 README。

验收标准：

1. `admin/` 可独立启动。
2. UI 风格为运营后台。
3. 不复活旧 React/Vite PWA MVP。

## 阶段 16A：最新内容与授权全文 PDF 采集设计

- [x] 补充 `docs/最新内容与授权全文PDF采集V1设计.md`。
- [x] 明确新闻默认只采集最近 24 小时。
- [x] 明确官方报告和授权 PDF 默认只采集最近 72 小时。
- [x] 明确不做历史批量回溯。
- [x] 设计来源许可策略和授权全文/PDF 数据模型。
- [x] 设计 Admin 合规审核与用户端展示边界。

验收标准：

1. 最新窗口、授权边界和 PDF 存储策略明确。
2. 付费研报、登录后 PDF、历史研报库仍明确禁止。
3. 后续实现可先从最新窗口过滤和来源许可字段开始。

阶段结果：已完成设计，并已落地后端基础能力：`news_ingestion_source` 增加来源许可字段和最新内容窗口字段，`RawNewsIngestionService` 在采集源已配置时会跳过超出窗口的内容；`report_asset` 已支持授权 PDF 元数据入库和文件哈希去重。授权全文字段和 PDF 文件缓存后续分阶段实现。

## 阶段 17：候选资讯审核 UI

- [ ] 候选资讯列表。
- [ ] 状态筛选。
- [ ] 候选详情预览。
- [ ] 编辑标题、摘要、分类、标签、来源。
- [ ] 拒绝候选。
- [ ] 发布为文章。
- [ ] 前端测试。

验收标准：

1. 运营人员可以从候选池发布文章。
2. 发布后 Flutter 用户端能读取新文章。
3. 拒绝内容不会出现在用户端。

## 阶段 18：每日简报生成与审核

- [ ] 从已发布文章中选择简报条目。
- [ ] Admin 编辑每日简报标题、副标题、摘要、热点清单。
- [ ] 发布为 `daily_digest`。
- [ ] Flutter 简报页读取真实发布结果。

验收标准：

1. 每日简报不再只依赖种子数据。
2. 简报发布后用户端今日简报可更新。
3. V1 不生成真实音频文件，仍使用客户端播报能力。

## 阶段 19：运营质量与监控

- [ ] 采集任务失败日志可查看。
- [ ] 采集源开关可配置。
- [ ] 记录发布操作日志。
- [ ] 基础统计：今日采集数、候选数、发布数、失败数。
- [ ] 异常数据检测：缺来源、缺链接、发布时间异常。

验收标准：

1. 采集失败可定位。
2. 发布行为可追溯。
3. 外部 API 接入可控、可回退。

## 阶段 20：三端收口与发布前检查

- [ ] 后端全量测试。
- [ ] Flutter analyze / test。
- [ ] Admin lint / test / build。
- [ ] OpenAPI 文档可访问。
- [ ] README 更新完整运行方式。
- [ ] 输出联调记录。
- [ ] 提交并推送 GitHub。

验收标准：

1. 三端工程可独立验证。
2. 真实资讯采集到发布闭环可演示。
3. 用户端仍稳定可用。

## 当前推荐执行顺序

1. 阶段 1 已完成。
2. 阶段 2 和阶段 3 已完成第一轮。
3. 阶段 4 已完成。
4. 阶段 5 已完成主干骨架。
5. 阶段 6 已完成 API Repository 真实接口冒烟和 Android 模拟器 API 模式页面级验收。
6. 阶段 7 已完成用户中心 API 与我的页真实数据接入。
7. 阶段 8 已完成收藏列表、阅读历史和播放历史闭环。
8. 阶段 9 已完成分页规范和列表管理能力。
9. 阶段 10 已完成 OpenAPI/Swagger 在线接口文档。
10. 阶段 11 已完成真实资讯采集 V1 设计。
11. 阶段 12 已完成采集适配层与首个 fixture Provider。
12. 阶段 13 已完成采集入库、任务日志与去重。
13. 阶段 14 已完成候选资讯生成。
14. 阶段 15 已完成 Admin API 审核发布设计。
15. 阶段 16A 已完成最新内容与授权全文 PDF 采集设计。
16. 下一阶段建议先实现最新窗口过滤和来源许可字段，再进入授权 PDF 元数据入库。
