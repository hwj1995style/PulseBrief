# PulseBrief 测试方案

## 测试目标

验证正式 V1 工程主线是否保持可构建、可测试，并为后续 Spring Boot、Flutter、MySQL 联调保留稳定回归基线。

## 自动化测试

### 后端测试

覆盖范围：

1. Spring Boot 应用入口存在且可调用。
2. `/api/health` 返回服务状态。
3. 分类、文章、简报、收藏、订阅、播放历史测试按 V1 接口分批补充。

V1 核心接口测试：

1. 分类：`GET /api/categories` 返回启用分类并按 `sortNo` 排序。
2. 首页：`GET /api/articles/home` 返回今日简报、投行观点推荐和资讯列表。
3. 文章：`GET /api/articles` 支持分类分页；`GET /api/articles/{id}` 返回结构化摘要且不返回新闻全文。
4. 简报：`GET /api/digests/today` 和 `GET /api/digests/{id}` 返回播报所需字段。
5. 登录：`POST /api/auth/login` 校验协议同意状态；`POST /api/auth/guest` 支持游客模式。
6. 订阅：未登录返回 401；登录后可查询和保存订阅。
7. 收藏：收藏与取消收藏保持幂等。
8. 播放历史：文章和简报播放记录均可写入。
9. Admin 发布候选后，用户端 `/api/articles` 和 `/api/articles/{id}` 能读取发布文章。
10. Admin 发布每日简报后，用户端 `/api/digests/today` 和 `/api/digests/{id}` 能读取发布简报。
11. Admin 采集监控接口可查询采集任务、失败错误信息、今日统计和采集源状态，并可切换采集源启停。
12. Admin 发布候选文章、发布每日简报和下线每日简报后会写入发布操作日志，且 `/api/admin/operation-logs` 可查询。
13. Admin 异常数据检测接口可返回原始资讯缺来源、缺原文链接、缺发布时间和未来发布时间异常。

数据库回归：

1. Flyway migration 在空库可初始化。
2. 种子数据包含分类、来源、文章和简报。
3. `news_article.article_hash` 唯一约束生效。

### Flutter 测试

覆盖范围：

1. 当前默认 widget 测试保持通过。
2. 后续补充首页、分类、详情、每日简报、收藏、播放器入口的 widget 测试。
3. UI 稳定后补充 golden 测试。

### Flutter Repository 数据层测试

覆盖范围：

1. 默认数据源为 mock，确保本机预览不依赖后端。
2. `PULSEBRIEF_DATA_SOURCE=api` 时创建 API repository。
3. API repository 可将 Spring Boot V1 响应映射为 Flutter UI model。
4. 登录、游客模式、分类、首页、简报、订阅快照均有最小单元测试。
5. 后台发布文章详情响应可映射为 APP `Article` 模型，包括 AI 摘要、核心要点和影响分析。
6. 后台发布简报响应可映射为 APP `TodayDigestFeed` 和 `Digest` 模型，包括热点清单和播报文案。

## 构建验证

每次交付前执行：

```powershell
.\scripts\check-env.ps1

.\scripts\use-jdk17.ps1
cd backend
.\mvnw.cmd test

cd ..\mobile
.\..\scripts\use-flutter.ps1
flutter test

cd ..
docker compose -f deploy/docker-compose.yml config
```

预期结果：

1. 所有测试通过。
2. 后端 Spring Boot 测试通过。
3. Flutter widget 测试通过。
4. Docker Compose 配置有效。

## CI 验证

CI 设计见 `docs/CI验证流水线设计.md`。

GitHub Actions 在 `main` 分支 push 和 pull request 时运行：

1. `backend` job：启动 MySQL 8.4 service container，执行 `backend/` 下的 `./mvnw test`。
2. `admin` job：执行 `npm ci`、`npm test -- --run`、`npm run lint` 和 `npm run build`。
3. `mobile` job：执行 `flutter pub get`、`flutter analyze` 和 `flutter test`。
4. `deploy-config` job：执行 `docker compose -f deploy/docker-compose.yml config`。

CI 默认不运行 Flutter live API 测试、不构建 Android APK、不接真实 Provider 或 AI 服务。需要真实密钥或外部网络的检查必须单独设计环境变量和跳过策略，不得直接写入 workflow。

## 真实 Provider 配置检查

真实 RSS/API Provider 接入前必须先通过本地配置检查，不得直接提交密钥或让 CI 请求外部数据源。

默认安全配置检查：

```powershell
.\scripts\check-provider-env.ps1
.\scripts\check-provider-env.ps1 -EnvFile .\.env.example
```

负向配置检查：

1. 启用 RSS 但使用 `example.com` 占位 URL，应失败。
2. 启用 API 但缺少 `PULSEBRIEF_PROVIDER_API_KEY`，应失败。
3. `PULSEBRIEF_INGESTION_ALLOW_BACKFILL=true` 应失败。
4. 全文或 PDF 能力开启但许可策略不匹配，应失败。

## 真实 RSS Provider V1 测试

阶段 23 自动化测试只使用本地 RSS/Atom fixture，不访问真实外网：

```powershell
cd backend
.\mvnw.cmd "-Dtest=RssNewsIngestionProviderTest,NewsIngestionProviderContextTest,RssNewsIngestionProviderLiveTest" test
```

预期结果：

1. RSS/Atom fixture 解析测试通过。
2. Spring 上下文同时注册 `FIXTURE` 和 `RSS` Provider。
3. `RssNewsIngestionProviderLiveTest` 默认跳过，不请求真实外网。

真实外网采集验证只作为本地手动 live smoke，必须显式开启：

```powershell
$env:PULSEBRIEF_RSS_LIVE_TEST_ENABLED='true'
$env:PULSEBRIEF_RSS_LIVE_TEST_URL='<public-rss-url>'
cd backend
.\mvnw.cmd "-Dtest=RssNewsIngestionProviderLiveTest" test
Remove-Item Env:PULSEBRIEF_RSS_LIVE_TEST_ENABLED
Remove-Item Env:PULSEBRIEF_RSS_LIVE_TEST_URL
```

阶段 23 本机验证记录：

1. 验证时间：2026-06-15。
2. 验证 URL：`https://rss.nytimes.com/services/xml/rss/nyt/World.xml`。
3. 验证命令：开启 `PULSEBRIEF_RSS_LIVE_TEST_ENABLED=true` 后执行 `RssNewsIngestionProviderLiveTest`。
4. 验证结果：Java RSS Provider 成功拉取真实外网 RSS，解析 1 到 5 条元数据并通过断言。
5. 网络说明：本机外网访问依赖 `HTTP_PROXY` / `HTTPS_PROXY`，`HttpRssFeedClient` 已读取这些环境变量；CI 仍不运行 live smoke。

live smoke 不进入 CI，不提交 `.env.local`、采集输出文件、PDF 或数据库 dump。

## Admin 手动触发真实 Provider 测试

阶段 24 自动化测试继续使用 fixture/mock，不访问真实外网：

```powershell
cd backend
.\mvnw.cmd "-Dtest=AdminIngestionControllerTest" test
.\mvnw.cmd test

cd ..\admin
npm test -- --run adminApi.test.ts
npm test -- --run App.test.tsx
npm test -- --run
npm run lint
npm run build
```

覆盖范围：

1. `POST /api/admin/ingestion/sources/{id}/run` 可触发已启用 source。
2. 手动触发成功后返回 jobId、sourceCode、providerType、状态和 fetched/new/duplicate/candidate 统计。
3. Provider 未注册时返回 422，并写入 FAILED job 与 errorMessage。
4. 超过 source 每小时限流时拒绝触发，且不新增 job。
5. Admin API client 可映射手动触发响应。
6. Admin mock client 可模拟手动触发并刷新任务与指标。
7. 采集监控页展示“手动采集”按钮，成功后刷新任务列表并显示成功提示。

阶段 24 本机真实手动触发 smoke：

1. 验证时间：2026-06-16。
2. 本地 source：`codex-stage24-rss`。
3. 验证 URL：`https://rss.nytimes.com/services/xml/rss/nyt/World.xml`。
4. 验证接口：`POST http://localhost:8081/api/admin/ingestion/sources/106/run`。
5. 请求参数：`pageSize=5`，`generateCandidates=true`。
6. 验证结果：jobId `349`，status `SUCCESS`，fetched `5`，new `5`，duplicate `0`，candidate `5`。
7. 数据库核对：`news_ingestion_job` 存在 `MANUAL/SUCCESS` 任务，`raw_news_item` 新增 5 条，`candidate_article` 生成 5 条。

阶段 24 smoke 仍不进入 CI，不提交本地数据库导出、采集输出文件、PDF 或真实生产配置。

## 授权正文片段与全文抓取服务测试

阶段 25 自动化测试必须使用本地 HTML fixture 或 Admin mock，不访问真实网页：

```powershell
cd backend
.\mvnw.cmd "-Dtest=ContentFetchServiceTest,AdminCandidateControllerTest,IngestionSchemaTest" test
.\mvnw.cmd test

cd ..\admin
npm test -- --run adminApi.test.ts App.test.tsx
npm test -- --run
npm run lint
npm run build
```

后端覆盖范围：

1. `SUMMARY_ONLY`、`LINK_ONLY`、`UNKNOWN` source 不保存正文。
2. `SNIPPET_ALLOWED` 且有授权说明的 source 可从本地 HTML fixture 提取正文片段。
3. `FULLTEXT_ALLOWED` 且有授权说明的 source 可从本地 HTML fixture 保存授权全文。
4. 缺少 `license_note` 时拒绝正文抓取。
5. 过期 raw item 不进入正文抓取。
6. 非 HTML、超大响应、空正文分别记录 `FAILED` 或 `SKIPPED`。
7. 重复正文通过 `content_text_hash` 去重。
8. 候选详情可返回正文预览、许可策略、授权说明、抓取状态和错误信息。
9. 发布接口不会把未审核正文直接发布到用户端。

Admin 覆盖范围：

1. 候选详情展示正文许可策略和授权说明。
2. 未授权 source 不展示正文抓取按钮，并展示不可抓取原因。
3. 授权 source 点击抓取后显示 loading、成功预览或失败原因。
4. mock client 支持正文抓取状态流转。

阶段 25 本机自动化验证记录：

1. `ContentFetchServiceTest` 覆盖授权策略、摘要-only 跳过、缺少授权说明、过期内容、HTML 提取、失败记录和全文模式。
2. `AdminCandidateControllerTest` 覆盖候选详情正文返回和手动抓取接口。
3. `IngestionSchemaTest` 覆盖 `raw_news_content` Flyway 表结构。
4. `adminApi.test.ts` 覆盖候选详情正文映射和正文抓取 POST 请求。
5. `App.test.tsx` 覆盖候选详情“授权正文”区块和 mock 抓取状态流转。

真实网页 smoke 只作为本地手动验证：

1. 必须显式开启 `PULSEBRIEF_CONTENT_FETCH_LIVE_TEST_ENABLED=true`。
2. 只允许公开、无需登录、授权明确的网页。
3. 单次最多抓取 1 到 3 条。
4. smoke 结果记录 source、candidate/raw item、抓取模式、正文长度和状态。
5. 不提交真实网页快照、正文输出、数据库 dump 或本地密钥。

CI 仍不访问真实网页，不依赖代理、AI Key、PDF 存储或外部正文解析服务。

## 授权 PDF 元数据与文件缓存测试

阶段 26 第一批实现已落地，自动化测试必须继续使用本地 fixture PDF 或 fake downloader，不访问真实 PDF URL：

```powershell
cd backend
.\mvnw.cmd "-Dtest=PdfAssetCacheServiceTest,AdminCandidateControllerTest,IngestionSchemaTest" test
.\mvnw.cmd test

cd ..\admin
npm test -- --run adminApi App
npm test -- --run
npm run lint
npm run build
```

后端覆盖范围：

1. `PDF_ALLOWED + allow_pdf_download=true + license_note` 可缓存 fixture PDF。
2. `PDF_ALLOWED` 但 `allow_pdf_download=false` 返回 `SKIPPED`，不写文件。
3. `SUMMARY_ONLY`、`LINK_ONLY`、`UNKNOWN` source 不缓存 PDF。
4. 缺少 `license_note` 时不缓存 PDF。
5. 超出 `max_age_hours` 的候选不缓存 PDF。
6. 非 PDF MIME 返回 `FAILED` 或 `SKIPPED`，不保留文件。
7. 超过大小限制时删除临时文件并记录失败。
8. 下载中断时删除临时文件并记录失败。
9. 相同文件哈希复用同一 `report_asset_file`。
10. 不同候选可拥有独立 `report_asset` 审核状态，同时复用同一物理文件。
11. 未审批 PDF 资产阻止 PDF 入口发布。
12. 拒绝 PDF 后候选可选择仅保留原文链接发布。

Admin 覆盖范围：

1. API client 映射 PDF 缓存状态、MIME、文件大小、授权说明和错误信息。
2. 候选详情展示“缓存 PDF”“审批 PDF”“拒绝 PDF”操作。
3. mock client 支持缓存成功、失败、审批和拒绝状态流转。
4. 发布被 PDF 合规状态阻断时展示原因。

阶段 26 第一批自动化验证记录：

1. `IngestionSchemaTest` 覆盖 `report_asset_file` 表和 `report_asset` 缓存/审核字段。
2. `PdfAssetCacheServiceTest` 覆盖授权缓存、未授权跳过和同一 PDF 物理文件复用。
3. `AdminCandidateControllerTest` 覆盖 PDF 缓存、审批、拒绝，以及审批/拒绝后候选发布行为。
4. `adminApi.test.ts` 覆盖 PDF 资产扩展字段映射和缓存/审批/拒绝端点。
5. `App.test.tsx` 覆盖候选详情 PDF 资产状态展示和 mock 状态流转。
6. `PULSEBRIEF_PDF_CACHE_ENABLED` 默认关闭；自动化测试显式开启并注入 fake downloader。

真实公开 PDF smoke 只作为本地手动验证：

1. 必须显式开启 `PULSEBRIEF_PDF_LIVE_TEST_ENABLED=true`。
2. 只允许公开、无需登录、授权明确的官方 PDF。
3. 单次最多缓存 1 个 PDF。
4. smoke 使用临时或本地开发存储目录。
5. smoke 记录 source、candidate、asset、文件大小、哈希、MIME 和状态。
6. 不提交 PDF 文件、缓存目录、数据库 dump、真实网页快照或本地密钥。

CI 仍不访问真实 PDF URL，不上传 PDF artifact，不依赖代理、对象存储、AI Key 或外部解析服务。

## AI 摘要任务与 Admin 审核入口测试

阶段 27 第一批实现使用 mock Provider，不访问真实 AI API，不读取未授权全文或 PDF 解析内容：

```powershell
cd backend
.\mvnw.cmd "-Dtest=AiSummaryTaskServiceTest,AdminCandidateControllerTest,IngestionSchemaTest" test
.\mvnw.cmd test

cd ..\admin
npm test -- --run adminApi.test.ts App.test.tsx
npm test -- --run
npm run lint
npm run build
```

后端覆盖范围：

1. 默认无 AI Key 时应用上下文启动成功。
2. `MOCK` Provider 可基于 RSS 摘要生成确定性草稿。
3. `CONTENT_SNIPPET` 成功时优先使用授权正文片段作为输入。
4. `CONTENT_FULLTEXT` 只有授权全文成功且许可说明存在时可用。
5. 未授权正文、未审核 PDF、登录后内容不进入 AI 输入。
6. 输入为空或过短时任务 `SKIPPED`。
7. Provider 抛错时任务 `FAILED`，候选仍可发布。
8. 任务记录保存输入来源、模型、prompt version、状态、错误信息和生成时间。
9. Admin 候选详情返回最新 AI 摘要任务。
10. Admin 发布接口仍使用人工确认后的请求字段，不自动采用最新 AI 结果。

Admin 覆盖范围：

1. API client 映射 AI 摘要任务状态和草稿字段。
2. 候选详情展示生成、重新生成和采用按钮。
3. mock client 支持生成成功、失败和采用到草稿。
4. AI 生成失败时页面保留候选摘要兜底。

真实 AI Provider smoke 只作为本地手动验证：

1. 必须显式开启 `PULSEBRIEF_AI_LIVE_TEST_ENABLED=true`。
2. 必须提供 `PULSEBRIEF_AI_PROVIDER`、`PULSEBRIEF_AI_MODEL` 和对应 API Key。
3. 只允许使用 fixture 或已授权输入。
4. 单次最多生成 1 条候选摘要。
5. 不提交 prompt 完整输入、模型响应原文、API Key、日志或数据库 dump。
6. smoke 不进入 CI。

CI 默认只运行 mock Provider，不注入真实 AI Key，不访问外部 AI API，不依赖真实 RSS、网页、PDF、代理或对象存储。

阶段 27 第一批实现记录：

1. 后端新增 `ai_summary_task`、`MOCK` Provider、任务服务和 Admin generate/apply API。
2. Admin 新增 AI 摘要生成、重新生成和采用草稿入口；发布时只提交 Admin 已采用或人工编辑后的草稿字段。
3. PDF 文本、真实 AI Provider、队列调度、重试和复杂分类仍属于后续阶段。
4. 已执行 targeted 验证：

```powershell
cd backend
.\mvnw.cmd "-Dtest=IngestionSchemaTest" test
.\mvnw.cmd "-Dtest=AiSummaryTaskServiceTest" test
.\mvnw.cmd "-Dtest=AdminCandidateControllerTest" test

cd ..\admin
npm test -- --run adminApi.test.ts App.test.tsx
```

## 手工回归清单

用户端：

1. Flutter App 可启动。
2. 下一阶段 UI 实现后，回归登录/兴趣选择、首页、分类、详情、简报、我的页面。
3. 收藏、播放、订阅等交互在 Flutter 内完成验证。

### Flutter 高保真 UI 精修回归

精修阶段以 Android 模拟器为主要视觉验收环境，Flutter Web 移动宽度预览作为辅助检查。

页面检查：

1. 登录页：品牌区、输入框、验证码按钮、主按钮、游客入口、协议勾选与原型接近。
2. 首页：顶部问候、今日全球简报主卡、分类 Tab、投行观点卡、热点资讯列表、底部导航不遮挡。
3. 分类页：横向分类导航、当前分类卡、今日重点双列卡、分类资讯列表与原型接近。
4. 订阅页：概览卡、推荐订阅网格、重点频道、推送偏好、底部保存区状态正常。
5. 详情页：AI 摘要、核心要点、可能影响、相关资讯、底部操作栏不溢出。
6. 简报页：今日早报主卡、简报列表、今日热点清单、底部导航高亮正确。
7. 播放器页：封面卡、进度条、播放控制、倍速卡、当前播报要点布局正常。
8. 我的页：头像昵称区、会员卡、菜单分组、简报偏好、底部协议链接和导航安全区正常。

交互检查：

1. 登录 / 游客模式进入首页。
2. 首页、分类、简报、我的底部导航切换。
3. 资讯卡片进入详情页。
4. 播放入口进入播放器页。
5. 收藏、播放暂停、倍速、分类、订阅标签和推送开关均可切换。

构建检查：

```powershell
.\scripts\use-flutter.ps1
cd mobile
flutter analyze
flutter test
flutter build apk --debug --target-platform android-x64
```

后台端：

1. React Admin 使用独立 `admin/` 工程，不复用已删除的旧 PWA。
2. API client 单测覆盖 mock/API 双模式的核心映射。
3. 真实 API 模式覆盖 `/api/admin/candidates` 列表、详情、发布和拒绝。
4. 后端回归覆盖 `/api/admin/**` 本地 Vite CORS 预检。
5. 采集任务页覆盖今日统计、失败日志、采集源状态展示、采集源启停交互、发布操作日志和异常数据检测展示。
6. 采集源卡片覆盖“手动采集”按钮：启用 source 可点击，停用 source 禁用；运行成功后刷新任务列表和今日指标，失败时展示错误提示。
7. 交付前执行：

```powershell
cd admin
npm test -- --run
npm run lint
npm run build

cd ..\backend
.\mvnw.cmd test
```

## 后续联调测试

后续补充：

1. Flutter Repository mock / API 双数据源切换测试。
2. MySQL schema migration 测试。
3. 采集任务去重测试。
4. AI 摘要长度与合规边界测试。
5. 推送任务限频测试。
6. 后台鉴权与操作日志测试。
